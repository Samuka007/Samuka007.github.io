import { nav } from './nav';
import { sidebar } from './sidebar';

export const themeConfig = {
  nav,
  sidebar,
  socialLinks: [{ icon: 'github', link: 'https://github.com/Samuka007' }],
  search: {
    provider: 'local',
  },
};
